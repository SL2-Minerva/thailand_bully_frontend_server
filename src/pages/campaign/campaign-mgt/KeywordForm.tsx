import {Ref, useState, forwardRef, ReactElement, useCallback, SyntheticEvent, useEffect} from 'react'


const KeywordForm = ({ indexNumber}) => {


  return (
    <div>input {`${indexNumber}`}</div>

  )
}
export default KeywordForm;
